---
title: v0 Roadmap
subtitle: On your marks
toc-title: "Contents"
...

# v0

## 0.1.0-dev.1 → 0.1.0

Release gate based on the [0.1.0-dev.1 release review](../reviews/release_review_0.1.0-dev.1.md).

### Blockers for 0.1.0

| # | Plan | Status | Notes |
|---|------|--------|-------|
| B1 | [WASM CI freshness check](../plans/completed/plan_betto_zstd_wasm_ci.md) | Done | `verify-wasm` CI job rebuilds and diffs `lib/assets/zstd.wasm` under pinned Emscripten; `test-web` gated on it |
| B2 | [iOS and Android CI coverage](../plans/plan_betto_zstd_mobile_ci.md) | Open | At minimum compile-only; full emulator run preferred |
| B3 | Pin `any` constraints in `pubspec.yaml` | Done | `code_assets ^1.0.0`, `hooks ^1.0.3`, `logging ^1.3.0`, `ffigen ^20.1.1` |

### Non-blocking (before pub.dev publish)

| # | Item | Status | Notes |
|---|------|--------|-------|
| N2 | Add `.pubignore` | Done | Excludes `integration_test_app/`, `src/`, `docs/`, `test/fixtures/`, `site/`, `coverage/` |
| N3 | Reconcile `init()` signatures | Open | Web has `wasmUrl` param; native/stub do not; spec updated to document this |
| N4 | Fix spec Public API section | Done | Removed unexported consts; corrected `init()` signature; added `ZstdException` |
| N5 | Remove unused constructor params | Done | Dropped `inputBufferLength` and `outputBufferLength` from all three implementations |
| N7 | Typed exception class | Done | `ZstdException implements Exception` added; all `throw Exception(...)` replaced |

### Deferred (post-0.1.0)

| Plan | Notes |
|------|-------|
| [Vendored C source update automation](../plans/plan_betto_zstd_vendor_automation.md) | `make update_zstd VERSION=x.y.z` target |
