---
title: v0 Roadmap
subtitle: On your marks
toc-title: "Contents"
...

# v0

## 0.1.0-dev.1 → 0.1.0

Release gate based on the [0.1.0-dev.1 release review](../reviews/release_review_0.1.0-dev.1.md).

### Blockers for 0.1.0

| # | Plan | Status | Notes |
|---|------|--------|-------|
| B1 | [WASM CI freshness check](../plans/plan_betto_zstd_wasm_ci.md) | Open | Prove committed WASM matches current `zstd.c` in CI |
| B2 | [iOS and Android CI coverage](../plans/plan_betto_zstd_mobile_ci.md) | Open | At minimum compile-only; full emulator run preferred |
| B3 | Pin `any` constraints in `pubspec.yaml` | Done | `code_assets ^1.0.0`, `hooks ^1.0.3`, `logging ^1.3.0`, `ffigen ^20.1.1` |

### Non-blocking (before pub.dev publish)

| # | Item | Notes |
|---|------|-------|
| N2 | Add `.pubignore` | Exclude `integration_test_app/`, `src/`, `docs/`, `test/fixtures/` |
| N3 | Reconcile `init()` signatures | Web has `wasmUrl` param; native/stub do not |
| N4 | Fix spec Public API section | `defaultLevel`, `maxInputBufferLength` are not in the `show` list |
| N5 | Remove or document unused constructor params | `inputBufferLength`, `outputBufferLength` |
| N7 | Typed exception class | Replace bare `Exception` with `ZstdException` |

### Deferred (post-0.1.0)

| Plan | Notes |
|------|-------|
| [Vendored C source update automation](../plans/plan_betto_zstd_vendor_automation.md) | `make update_zstd VERSION=x.y.z` target |
